namespace :create_download_task do

	desc "Task description"
	# bundle exec rake create_download_task:download_all_dataset RAILS_ENV=production
	task :download_all_dataset => [:environment] do
		
	end
end